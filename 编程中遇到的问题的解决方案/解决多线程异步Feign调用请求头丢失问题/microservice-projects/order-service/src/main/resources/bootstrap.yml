spring:
  application:
    name: order-service
  cloud:
    nacos:
      discovery:
        server-addr: 127.0.0.1:8848

# 暴露监控端点
management:
  endpoints:
    web:
      exposure:
        include: '*'
